- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux: tmux
    ~/.tmux.conf: tmux/tmux.conf
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
    ~/.oh-my-zsh: oh-my-zsh
    ~/.zsh: zsh
    ~/.zshrc: zshrc
    oh-my-zsh/themes/powerlevel9k: powerlevel9k

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [./patch_gitconfig.sh >> ~/.gitconfig, Patching ~/.gitconfig]
